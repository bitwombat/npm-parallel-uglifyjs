{
  "name": "parallel-uglifyjs",
  "description": "Recursively trawls a directory for .js files to uglify, running one node instance per available CPU.",
  "license": "GPL-3.0",
  "version": "1.0.9",
  "author": "Greg Bell <parallel-uglifyjs@bitwombat.com.au>",
  "dependencies": {
    "finder-on-steroids": "^0.3.1",
    "uglify-js": "~2.6.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bitwombat/parallel-uglifyjs"
  },
  "bin": {
    "parallel-uglifyjs": "./cli/parallel-uglifyjs.js"
  },
  "bugs": {
    "url": "https://github.com/bitwombat/parallel-uglifyjs/issues"
  },
  "keywords": [
    "multicore",
    "parallel",
    "fork",
    "uglifyjs",
    "uglify-js"
  ],
  "scripts": {
    "test": "./cli/parallel-uglifyjs.js small_test",
    "big_test": "./cli/parallel-uglifyjs.js big_test"
  }
}
